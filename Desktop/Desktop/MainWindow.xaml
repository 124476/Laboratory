<Window x:Class="Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Desktop"
        mc:Ignorable="d"
        Title="Лаборатория" Height="450" Width="800"
        MinHeight="450" MinWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Name="StackHeader" Orientation="Horizontal" HorizontalAlignment="Right">
            <Image Source="{Binding Photo}" Height="50" Width="50"></Image>
            <StackPanel Margin="10,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Surname}"></TextBlock>
                    <TextBlock Text="{Binding Name}" VerticalAlignment="Center"></TextBlock>
                </StackPanel>
                <TextBlock Text="{Binding Role.Name}"></TextBlock>
            </StackPanel>
            <Button Name="BtnLogout" Click="BtnLogout_Click" Width="150" Margin="10">Выйти</Button>
        </StackPanel>
        <Frame Name="MyFrame" Grid.Row="1" NavigationUIVisibility="Hidden" ContentRendered="MyFrame_ContentRendered"></Frame>
    </Grid>
</Window>
